/**
 * 아이디 중복
 */
document.addEventListener('DOMContentLoaded', () => {

	/*$("#idcf").click(function() {
		$("#signup").attr("type", "button");
		const id = $("#userid").val();
		$.ajax({
			type: "get",
			async: true,
			url: "http://localhost:8090/api/member/idCheck",
			data: { id: id },
			success: function(data) {
				if (data == 1) {
					$("#cfmessage").text("이미 사용중인 ID 입니다.");
				} else {
					$("#cfmessage").text("사용 가능한 ID 입니다.");
				$("#signup").attr("type", "submit");
						}
				}
		  });
	});*/
	
});
